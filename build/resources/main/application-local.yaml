server:
  port: 8080
logging:
  config: classpath:logback-spring-local.xml
  level:
    org:
      hibernate:
        SQL: DEBUG
spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    username: "root"
    password: "asdf1234"
    url: "jdbc:mysql://localhost:3306/week_1?useUnicode=true&characterEncoding=UTF-8"
    driver-class-name: "com.mysql.cj.jdbc.Driver"